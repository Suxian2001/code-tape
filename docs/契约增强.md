# GitNexus + OpenViking 契约增强

本文说明 code-tape 的双层契约：本地 agent 先用 GitNexus 建立代码图谱并做影响分析，PR 再由 CI 阻断兜底；OpenViking 保存团队共享的权威项目知识。

## 本地 agent 工作流

所有 agent 在改代码前先运行：

```bash
npm run agent:bootstrap
```

每次开始任务和提交前运行：

```bash
npm run contract:local
```

`contract:local` 会强制执行 `npx --yes gitnexus@1.6.5 analyze --force --index-only`，只刷新 `.gitnexus/` 索引，不注入或重写 `AGENTS.md` / `CLAUDE.md`。随后脚本检查当前 diff 是否触碰关键骨架。命中以下区域时，必须阅读脚本输出的 GitNexus 建议，并在 PR 的 “GitNexus 影响分析摘要” 中写明结论：

- `apps/web/src/shared/recording-schema/`
- `apps/web/src/features/runtime-preview/`
- `apps/web/src/features/library/recordingStore*`
- `apps/web/src/features/player/replay*`、`packageLoader*`、`timelineClock*`、`mediaClockAdapter*`
- `.github/workflows/`、`scripts/workflows/`
- `README.md`、`AGENTS.md`、`CLAUDE.md`、PRD、技术方案、流程文档

本仓库不自动安装 git hooks，避免改动组员个人环境；契约通过 agent 指令、npm scripts、PR 模板和 CI 共同生效。

## PR / CI 契约

PR 阶段会运行：

```bash
npm run contract:gitnexus
npm run contract:openviking:check
```

`contract:gitnexus` 同样强制刷新 GitNexus 索引。关键骨架变更必须同时满足：

- 有匹配的契约测试或 workflow 测试变更。
- PR 模板中填写了 GitNexus 影响分析摘要。

`contract:openviking:check` 只校验 `docs/contracts/openviking.resources.json`，不会上传 PR 内容到 OpenViking。

## OpenViking 服务

OpenViking 作为项目知识库运行在维护者本机，由自托管 runner 访问。CI 需要配置：

- `vars.OPENVIKING_BASE_URL`
- `secrets.OPENVIKING_API_KEY`
- `OPENVIKING_ACCOUNT=code-tape`
- `OPENVIKING_USER=code-tape-ci`
- `OPENVIKING_AGENT_ID=github-actions`

维护者本机的默认服务地址为 `http://127.0.0.1:1933`。本机私有配置放在 `~/.openviking/ov.conf`、`~/.openviking/ovcli.conf` 和仓库根目录 `.env.openviking.local`，其中 `.env.openviking.local` 提供 `OPENVIKING_BASE_URL`、`OPENVIKING_API_KEY`、`OPENVIKING_ACCOUNT`、`OPENVIKING_USER`、`OPENVIKING_AGENT_ID` 给本地 `npm run contract:openviking:sync` 使用；这些文件不入库。

维护者本机服务由用户级 `launchd` 管理：

- `~/Library/LaunchAgents/com.code-tape.openviking-embedding.plist` 启动本地 embedding shim，监听 `127.0.0.1:1932`。
- `~/Library/LaunchAgents/com.code-tape.openviking-server.plist` 启动 OpenViking server，监听 `127.0.0.1:1933`。

自托管 runner 默认 label：

```text
self-hosted, macOS, repo-guard-intranet
```

合入 `main` 或手动触发 workflow 后，同步命令为：

```bash
npm run contract:openviking:sync
```

维护者本机可以把同名变量写入不会提交的 `.env.openviking.local`。`contract:openviking:sync` 会在非 CI 环境自动读取该文件，且不会覆盖 shell 中已经显式设置的环境变量。

该命令会先执行 manifest 校验，再执行 `ov health`；同步资源时会把清单中的 `/projects/code-tape/...` 逻辑 URI 映射到 OpenViking 数据层的 `viking://resources/projects/code-tape/...`。目标已存在则先按资源类型删除 manifest 管理的旧资源：目录使用 `ov rm --recursive <uri>`，文件使用 `ov rm <uri>`；随后用 `ov write --from-file <path> --mode create --wait` 写入精确文件 URI。如果 OpenViking 服务不可达，PR health gate 或 main sync 会失败，需要维护者先恢复本机服务。

## 资源清单维护

`docs/contracts/openviking.resources.json` 是 OpenViking 项目知识库入口。任何权威资源内容变化后，都必须更新对应 `sha256`。

更新步骤：

1. 修改文档或规则。
2. 重新计算变更文件的 sha256。
3. 更新 `docs/contracts/openviking.resources.json`。
4. 运行 `npm run contract:openviking:check`。

PR 阶段不上传未合并内容，合入 main 后再同步，避免共享知识库被临时 PR 状态污染。
